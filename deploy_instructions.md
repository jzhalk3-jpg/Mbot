دليل سريع لتشغيل ونشر بوت تلغرام ذكي

ملخص
- هذا المشروع يوفّر بوت تلغرام بسيط يرسل رسائل المستخدم إلى OpenAI Chat API (مثل gpt-3.5-turbo) ويعيد الرد.

ملفات مهمة
- bot.py: الكود الرئيسي
- .env.example: أمثلة على المتغيرات البيئية (لا تضع مفاتيح حقيقية في هذا الملف)
- requirements.txt: تبعيات بايثون
- Dockerfile / Procfile: للنشر في حاويات أو Heroku

المتطلبات
- Python 3.10/3.11
- حساب OpenAI مع مفتاح API
- توكن بوت تلغرام

كيفية الاستخدام محلياً
1) انسخ .env.example إلى .env واملأ القيم:
   TELEGRAM_TOKEN=ضع_هنا_توكن_البوت
   OPENAI_API_KEY=ضع_هنا_مفتاح_OpenAI
2) ثبت المتطلبات:
   pip install -r requirements.txt
3) شغّل البوت:
   python bot.py

أمان
- لا ترفع التوكن أو مفاتيح OpenAI إلى المستودع. استعمل GitHub Secrets أو متغيرات البيئة على الخادم/الخدمة المستضيفة.

نشر (مقترح)
- Heroku: اضبط Config Vars (TELEGRAM_TOKEN, OPENAI_API_KEY)، ثم ادفع الكود. Heroku سيقرأ Procfile ويشغّل البوت.
- Docker: ابني الصورة وشغّلها مع تمرير المتغيرات:
   docker build -t mbot .
   docker run -e TELEGRAM_TOKEN=... -e OPENAI_API_KEY=... mbot

ملاحظات حول المفاتيح التي زوّدتني بها
- لم أقم بإدراج أي من التوكنات أو المفاتيح التي أرسلتها داخل المستودع لأن هذا يمثل خطر أمني.
- إذا أردت أن أضيفها كمثال داخل ملف .env.example أو كـ GitHub Secret، أخبرني كيف تود ذلك. يُفضل أن تضيفها بنفسك إلى إعدادات المستودع (Settings -> Secrets) أو إلى الخادم/الخدمة التي ستشغّل البوت.

مساعدة إضافية
- إن أردت أن أدفع التغييرات الآن إلى الفرع الخاص بك وأنشئ ملف .env يحتوي على التوكن (غير آمن)، أخبرني صراحة ووافق على أن أضع الأسرار في المستودع (لن أفعل ذلك افتراضياً).
